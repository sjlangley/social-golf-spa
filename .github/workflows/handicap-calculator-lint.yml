name: Handicap Calculator Lint

on:
  push:
    branches: [main]
    paths:
      - 'apps/handicap-calculator/**'
      - '!apps/handicap-calculator/*.md'
      - '!apps/handicap-calculator/**/*.md'
      - '.github/workflows/handicap-calculator-lint.yml'
  pull_request:
    branches: [main]
    paths:
      - 'apps/handicap-calculator/**'
      - '!apps/handicap-calculator/*.md'
      - '!apps/handicap-calculator/**/*.md'
      - '.github/workflows/handicap-calculator-lint.yml'

jobs:
  lint:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    defaults:
      run:
        working-directory: apps/handicap-calculator

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: 'apps/handicap-calculator/pyproject.toml'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Check code style with ruff
        run: ruff check src/

      - name: Check code formatting with ruff
        run: ruff format --check src/

      - name: Check type safety with pyrefly
        run: pyrefly check src/
